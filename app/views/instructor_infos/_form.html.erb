<%= form_with(model: instructor_info) do |form| %>
  <% if instructor_info.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(instructor_info.errors.count, "error") %> prohibited this instructor_info from being saved:</h2>

      <ul>
        <% instructor_info.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :profile_img_path, style: "display: block" %>
    <%= form.text_field :profile_img_path %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
